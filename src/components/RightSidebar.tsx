import React from "react"; // Import React

export default function RightSidebar() {
  return (
    // Right sidebar container styling from image - content removed
    <div className="w-64 border-l border-gray-800 p-4 flex flex-col bg-gray-950 text-gray-100">
      {/* Content removed as requested */}
    </div>
  );
}
